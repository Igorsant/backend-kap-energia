Name: Test_workflow
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    Branches:
      - test/aws-workflows

# Required - Define action configurations.
Actions:
    BuildAndTest:
        Identifier: aws/build@v1
        Inputs:
            Sources:
            - WorkflowSource
        Configuration:
            Steps:
            - Run: echo "Hello Test Workflow"
            - Run: npm --version